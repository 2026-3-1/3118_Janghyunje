# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tc05-coach-dashboard.spec.js >> TC-05. 코치 강의 등록 → 수강자 목록 확인 >> 강의 등록 페이지 접근 성공
- Location: tests\tc05-coach-dashboard.spec.js:42:7

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/login
Call log:
  - navigating to "http://localhost:5173/login", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | const COACH_EMAIL = 'Kid1223@test.com'
  4  | const COACH_PW    = 'Test1234!'
  5  | 
  6  | test.describe('TC-05. 코치 강의 등록 → 수강자 목록 확인', () => {
  7  | 
  8  |   test.beforeEach(async ({ page }) => {
> 9  |     await page.goto('/login')
     |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/login
  10 |     await page.getByPlaceholder(/email/i).fill(COACH_EMAIL)
  11 |     await page.getByPlaceholder(/비밀번호/i).fill(COACH_PW)
  12 |     await page.getByRole('button', { name: '로그인' }).click()
  13 |     await expect(page).toHaveURL('/')
  14 | 
  15 |     const user = await page.evaluate(() => JSON.parse(localStorage.getItem('user') || 'null'))
  16 |     expect(user?.role).toBe('coach')
  17 |   })
  18 | 
  19 |   test('코치 대시보드 접근 성공', async ({ page }) => {
  20 |     await page.goto('/coach/dashboard')
  21 |     await expect(page).toHaveURL('/coach/dashboard')
  22 |     await expect(page.getByText('코치 대시보드')).toBeVisible()
  23 |   })
  24 | 
  25 |   test('내 강의 목록 탭 — 다른 코치 강의 없음', async ({ page }) => {
  26 |     await page.goto('/coach/dashboard')
  27 | 
  28 |     // 내 강의 목록 탭 클릭
  29 |     await page.getByRole('button', { name: '내 강의 목록' }).click()
  30 |     await page.waitForTimeout(500)
  31 | 
  32 |     // 강의가 있으면 모두 본인 강의인지 확인
  33 |     const lectureCards = page.locator('[class*="rounded-xl"][class*="border"]')
  34 |     const count = await lectureCards.count()
  35 | 
  36 |     if (count > 0) {
  37 |       // 각 카드에 수강자 보기 버튼이 있는지 확인
  38 |       await expect(page.getByRole('button', { name: '수강자 보기' }).first()).toBeVisible()
  39 |     }
  40 |   })
  41 | 
  42 |   test('강의 등록 페이지 접근 성공', async ({ page }) => {
  43 |     await page.goto('/coach/lecture/new')
  44 |     await expect(page).toHaveURL('/coach/lecture/new')
  45 |     await expect(page.getByText(/강의 등록|강의 정보/)).toBeVisible()
  46 |   })
  47 | 
  48 |   test('수강자 목록 탭 — 강의 선택 후 수강자 확인', async ({ page }) => {
  49 |     await page.goto('/coach/dashboard')
  50 | 
  51 |     // 수강자 목록 탭 클릭
  52 |     await page.getByRole('button', { name: '수강자 목록' }).click()
  53 |     await page.waitForTimeout(500)
  54 | 
  55 |     // 강의 목록이 표시되는지 확인
  56 |     await expect(page.getByText(/수강자를 확인할 강의를 선택|강의가 없습니다/)).toBeVisible()
  57 | 
  58 |     // 강의가 있으면 첫 번째 강의 클릭
  59 |     const lectureBtn = page.getByRole('button').filter({ hasText: /LOL|발로란트|오버워치|배그|TFT|스타/ }).first()
  60 |     const hasLecture = await lectureBtn.isVisible().catch(() => false)
  61 | 
  62 |     if (hasLecture) {
  63 |       await lectureBtn.click()
  64 |       await page.waitForTimeout(500)
  65 |       // 수강자 목록 또는 빈 상태 표시 확인
  66 |       await expect(page.getByText(/수강 중|수강자가 없습니다/)).toBeVisible()
  67 |     }
  68 |   })
  69 | })
  70 | 
```