# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tc03-student-role-guard.spec.js >> TC-03. 학생이 코치 전용 페이지 접근 차단 >> 강의 등록 페이지 → / 리다이렉트
- Location: tests\tc03-student-role-guard.spec.js:27:7

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/login
Call log:
  - navigating to "http://localhost:5173/login", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | // 학생 계정 (사전에 DB에 존재해야 함)
  4  | const STUDENT_EMAIL = 'student_test@test.com'
  5  | const STUDENT_PW    = 'Test1234!'
  6  | 
  7  | test.describe('TC-03. 학생이 코치 전용 페이지 접근 차단', () => {
  8  | 
  9  |   test.beforeEach(async ({ page }) => {
  10 |     // 학생으로 로그인
> 11 |     await page.goto('/login')
     |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/login
  12 |     await page.getByPlaceholder(/email/i).fill(STUDENT_EMAIL)
  13 |     await page.getByPlaceholder(/비밀번호/i).fill(STUDENT_PW)
  14 |     await page.getByRole('button', { name: '로그인' }).click()
  15 |     await expect(page).toHaveURL('/')
  16 | 
  17 |     // 학생 계정인지 확인
  18 |     const user = await page.evaluate(() => JSON.parse(localStorage.getItem('user') || 'null'))
  19 |     expect(user?.role).toBe('student')
  20 |   })
  21 | 
  22 |   test('코치 대시보드 → / 리다이렉트', async ({ page }) => {
  23 |     await page.goto('/coach/dashboard')
  24 |     await expect(page).toHaveURL('/')
  25 |   })
  26 | 
  27 |   test('강의 등록 페이지 → / 리다이렉트', async ({ page }) => {
  28 |     await page.goto('/coach/lecture/new')
  29 |     await expect(page).toHaveURL('/')
  30 |   })
  31 | 
  32 |   test('학생 전용 페이지는 정상 접근', async ({ page }) => {
  33 |     await page.goto('/mypage')
  34 |     await expect(page).toHaveURL('/mypage')
  35 | 
  36 |     await page.goto('/cart')
  37 |     await expect(page).toHaveURL('/cart')
  38 | 
  39 |     await page.goto('/growth')
  40 |     await expect(page).toHaveURL('/growth')
  41 |   })
  42 | })
  43 | 
```