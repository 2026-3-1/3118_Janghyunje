# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tc04-cart-checkout.spec.js >> TC-04. 장바구니 담기 → 결제 흐름 >> 강의 상세에서 장바구니 담기 → Navbar 뱃지 표시
- Location: tests\tc04-cart-checkout.spec.js:16:7

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/login
Call log:
  - navigating to "http://localhost:5173/login", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | const STUDENT_EMAIL = 'student_test@test.com'
  4  | const STUDENT_PW    = 'Test1234!'
  5  | 
  6  | test.describe('TC-04. 장바구니 담기 → 결제 흐름', () => {
  7  | 
  8  |   test.beforeEach(async ({ page }) => {
> 9  |     await page.goto('/login')
     |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/login
  10 |     await page.getByPlaceholder(/email/i).fill(STUDENT_EMAIL)
  11 |     await page.getByPlaceholder(/비밀번호/i).fill(STUDENT_PW)
  12 |     await page.getByRole('button', { name: '로그인' }).click()
  13 |     await expect(page).toHaveURL('/')
  14 |   })
  15 | 
  16 |   test('강의 상세에서 장바구니 담기 → Navbar 뱃지 표시', async ({ page }) => {
  17 |     // 강의 상세 접근
  18 |     await page.goto('/lectures')
  19 |     await page.locator('.grid > div').first().click()
  20 |     await expect(page).toHaveURL(/\/lectures\/\d+/)
  21 | 
  22 |     // 이미 수강 중이면 테스트 스킵 가능
  23 |     const isEnrolled = await page.getByText('수강 중').isVisible().catch(() => false)
  24 |     if (isEnrolled) {
  25 |       test.skip()
  26 |       return
  27 |     }
  28 | 
  29 |     // 장바구니 담기 버튼 클릭
  30 |     await page.getByRole('button', { name: /장바구니에 담기/ }).click()
  31 | 
  32 |     // 토스트 메시지 확인
  33 |     await expect(page.getByText(/장바구니에 담겼습니다/)).toBeVisible()
  34 | 
  35 |     // Navbar 뱃지 확인
  36 |     await expect(page.locator('nav').getByText(/[1-9]/)).toBeVisible()
  37 |   })
  38 | 
  39 |   test('장바구니 페이지에서 강의 확인 및 결제 페이지 이동', async ({ page }) => {
  40 |     await page.goto('/cart')
  41 |     await expect(page).toHaveURL('/cart')
  42 |     await expect(page.getByText('장바구니')).toBeVisible()
  43 | 
  44 |     // 아이템이 있으면 결제 버튼 클릭
  45 |     const hasItems = await page.getByText(/결제하기/).isVisible().catch(() => false)
  46 |     if (hasItems) {
  47 |       await page.getByRole('button', { name: /결제하기/ }).click()
  48 |       await expect(page).toHaveURL('/checkout')
  49 |       await expect(page.getByText('결제하기')).toBeVisible()
  50 |     }
  51 |   })
  52 | 
  53 |   test('장바구니에서 강의 삭제', async ({ page }) => {
  54 |     await page.goto('/cart')
  55 | 
  56 |     const removeBtn = page.getByRole('button', { name: '✕' }).first()
  57 |     const hasItems  = await removeBtn.isVisible().catch(() => false)
  58 | 
  59 |     if (hasItems) {
  60 |       const countBefore = await page.locator('.grid > div, [class*="border"]').count()
  61 |       await removeBtn.click()
  62 |       // 삭제 후 아이템 수 감소 확인
  63 |       await page.waitForTimeout(500)
  64 |       const countAfter = await page.locator('.grid > div, [class*="border"]').count()
  65 |       expect(countAfter).toBeLessThanOrEqual(countBefore)
  66 |     }
  67 |   })
  68 | })
  69 | 
```