# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tc01-signup-login-checkout.spec.js >> TC-01. 회원가입 → 로그인 → 강의 결제 흐름 (학생) >> 회원가입 성공
- Location: tests\tc01-signup-login-checkout.spec.js:11:7

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/register
Call log:
  - navigating to "http://localhost:5173/register", waiting until "load"

```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test'
  2   | 
  3   | // 테스트마다 고유한 이메일 생성
  4   | const timestamp  = Date.now()
  5   | const testEmail  = `test_${timestamp}@test.com`
  6   | const testPw     = 'Test1234!'
  7   | const testNick   = `tester_${timestamp}`
  8   | 
  9   | test.describe('TC-01. 회원가입 → 로그인 → 강의 결제 흐름 (학생)', () => {
  10  | 
  11  |   test('회원가입 성공', async ({ page }) => {
> 12  |     await page.goto('/register')
      |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/register
  13  | 
  14  |     // 학생 역할 선택
  15  |     await page.getByRole('button', { name: '학생' }).click()
  16  | 
  17  |     // 폼 입력
  18  |     await page.getByPlaceholder(/email/i).fill(testEmail)
  19  |     await page.getByPlaceholder(/닉네임|아이디/i).fill(testNick)
  20  | 
  21  |     const pwFields = page.getByPlaceholder(/비밀번호/i)
  22  |     await pwFields.nth(0).fill(testPw)
  23  |     await pwFields.nth(1).fill(testPw)
  24  | 
  25  |     // 회원가입 버튼
  26  |     await page.getByRole('button', { name: '회원가입' }).click()
  27  | 
  28  |     // 로그인 페이지로 이동 확인
  29  |     await expect(page).toHaveURL(/login/)
  30  |   })
  31  | 
  32  |   test('로그인 후 JWT 토큰 localStorage 저장 확인', async ({ page }) => {
  33  |     await page.goto('/login')
  34  | 
  35  |     await page.getByPlaceholder(/email/i).fill(testEmail)
  36  |     await page.getByPlaceholder(/비밀번호/i).fill(testPw)
  37  |     await page.getByRole('button', { name: '로그인' }).click()
  38  | 
  39  |     // 메인 페이지 이동 확인
  40  |     await expect(page).toHaveURL('/')
  41  | 
  42  |     // localStorage token 확인
  43  |     const token = await page.evaluate(() => localStorage.getItem('token'))
  44  |     expect(token).toBeTruthy()
  45  | 
  46  |     // localStorage user 확인
  47  |     const user = await page.evaluate(() => JSON.parse(localStorage.getItem('user') || 'null'))
  48  |     expect(user).not.toBeNull()
  49  |     expect(user.role).toBe('student')
  50  |   })
  51  | 
  52  |   test('강의 상세에서 결제 페이지로 이동', async ({ page }) => {
  53  |     // 로그인
  54  |     await page.goto('/login')
  55  |     await page.getByPlaceholder(/email/i).fill(testEmail)
  56  |     await page.getByPlaceholder(/비밀번호/i).fill(testPw)
  57  |     await page.getByRole('button', { name: '로그인' }).click()
  58  |     await expect(page).toHaveURL('/')
  59  | 
  60  |     // 강의 목록으로 이동
  61  |     await page.goto('/lectures')
  62  |     await expect(page.locator('.grid')).toBeVisible()
  63  | 
  64  |     // 첫 번째 강의 클릭
  65  |     const firstCard = page.locator('.grid > div').first()
  66  |     await firstCard.click()
  67  |     await expect(page).toHaveURL(/\/lectures\/\d+/)
  68  | 
  69  |     // 결제 버튼 클릭
  70  |     await page.getByRole('button', { name: /결제하기/ }).click()
  71  |     await expect(page).toHaveURL('/checkout')
  72  | 
  73  |     // 결제 페이지 요소 확인
  74  |     await expect(page.getByText('결제하기')).toBeVisible()
  75  |     await expect(page.getByText(/수강 신청 강의/)).toBeVisible()
  76  |   })
  77  | 
  78  |   test('결제 완료 후 마이페이지에 수강 중 표시', async ({ page }) => {
  79  |     await page.goto('/login')
  80  |     await page.getByPlaceholder(/email/i).fill(testEmail)
  81  |     await page.getByPlaceholder(/비밀번호/i).fill(testPw)
  82  |     await page.getByRole('button', { name: '로그인' }).click()
  83  |     await expect(page).toHaveURL('/')
  84  | 
  85  |     // 강의 상세 → 결제
  86  |     await page.goto('/lectures')
  87  |     await page.locator('.grid > div').first().click()
  88  |     await page.getByRole('button', { name: /결제하기/ }).click()
  89  |     await expect(page).toHaveURL('/checkout')
  90  | 
  91  |     // 약관 동의
  92  |     await page.getByRole('checkbox').click()
  93  | 
  94  |     // 결제 버튼
  95  |     await page.getByRole('button', { name: /원 결제하기/ }).click()
  96  | 
  97  |     // 결제 완료 화면 확인
  98  |     await expect(page.getByText('결제 완료!')).toBeVisible()
  99  | 
  100 |     // 마이페이지 이동
  101 |     await page.getByRole('button', { name: '내 수강 목록' }).click()
  102 |     await expect(page).toHaveURL('/mypage')
  103 | 
  104 |     // 수강 중 확인
  105 |     await expect(page.getByText('수강 중').first()).toBeVisible()
  106 |   })
  107 | })
  108 | 
```